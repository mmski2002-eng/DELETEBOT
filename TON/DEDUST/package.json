{
  "name": "dedust-audit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --runInBand --forceExit",
    "test:crit01": "jest --testPathPattern=crit01 --runInBand --forceExit --verbose",
    "test:crit02": "jest --testPathPattern=crit02 --runInBand --forceExit --verbose",
    "test:crit03": "jest --testPathPattern=crit03 --runInBand --forceExit --verbose",
    "test:high04": "jest --testPathPattern=high04 --runInBand --forceExit --verbose"
  },
  "devDependencies": {
    "@tact-lang/opcode": "^0.3.2",
    "@ton/blueprint": "^0.27.0",
    "@ton/core": "^0.63.0",
    "@ton/crypto": "^3.3.0",
    "@ton/sandbox": "^0.23.0",
    "@ton/test-utils": "^0.4.2",
    "@ton/ton": "^15.1.0",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ton-disassembler": "^1.1.1",
    "ts-jest": "^29.2.5",
    "typescript": "^5.4.5"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testTimeout": 60000,
    "globals": {
      "ts-jest": {
        "tsconfig": "tsconfig.json"
      }
    }
  },
  "dependencies": {
    "@dedust/sdk": "^0.8.7"
  }
}
