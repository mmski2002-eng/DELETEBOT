[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]
test = "test"

# Solidity version matching the PoC
solc_version = "0.8.26"

# Gas reporting
gas_reports = ["*"]

# For better stack traces
ffi = true
